{
    "config": {
        "vendor-dir": "application/vendor"
    },
    "require": {
        "90zlaya/php-library": "^1.4"
    },
    "require-dev": {
        "kenjis/ci-phpunit-test": "^0.15.0",
        "squizlabs/php_codesniffer": "^3.2",
        "phpstan/phpstan": "^0.9.1"
    },
    "scripts": {
        "phpstan": "application/vendor/bin/phpstan analyse -c phpstan.neon -l max application/controllers application/models application/tests"
    }
}
